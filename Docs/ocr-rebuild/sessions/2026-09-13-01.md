# Sesija 01 — 2026-09-13

## Tikslas

Modulio analizė, plano sudarymas, dokumentacijos karkasas, inventorizacijos
užduoties paleidimas.

## Ką darėm

- Perskaityta: `ExpenseOcrService.cs` (~890 eil.), `OcrQueueWorker.cs`,
  `IExpenseOcrService.cs`, `OcrResultDto.cs`, `ExpenseInvoice.cs`,
  `ExpenseModels.cs`, `ExpenseUploadDialog.razor` code-behind, `Program.cs` DI.
- Rasta 18 radinių: 4 P0, 8 P1 (duomenų teisingumas), 6 P2 (architektūra).
  Santrauka — `DECISIONS.md` D-001.
- Sudarytas pilnas restruktūrizacijos planas su premortem ir redteam — `PLAN.md`.
- Sukurtas `Docs/ocr-rebuild/` karkasas.
- Į `.opencode/tasks/latest.md` įrašyta inventorizacijos užduotis (read-only).

## Svarbiausias radinys

Globalus `NoTracking` + `OcrQueueWorker` statuso rašymas per `SaveChangesAsync`
= begalinis ciklas, kuris kas 30 s kviečia Azure ant tos pačios eilutės. Tai
paaiškina „Azure sunkiai veikia" — problema ne modelio kokybėje, o kvotos
deginime.

## Nepadaryta / atidėta

- DB auditas — reikalauja produkcijos užklausų, vykdo Deividas (Q-001, Q-003).
- F0 build prompt'as — laukia inventorizacijos ataskaitos.

## Kitos sesijos pradžia

Perskaityti inventorizacijos ataskaitą iš `.opencode/reports/`, patvirtinti P0
sąrašą, rašyti F0 build prompt'ą (`paid_amount`).
