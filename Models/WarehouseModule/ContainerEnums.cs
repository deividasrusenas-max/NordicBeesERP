namespace NordicBeesERP.Models.WarehouseModule;

public enum ContainerType { BARREL, BUCKET_GROUP }

public enum ContainerStatus { RECEIVED, IN_STOCK, RESERVED, IN_PRODUCTION, SOLD, RETURNED, WRITTEN_OFF }
