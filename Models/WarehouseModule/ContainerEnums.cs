namespace NordicBeesERP.Models.WarehouseModule;

public enum ContainerType { BARREL, BUCKET }

public enum ContainerStatus { RECEIVED, IN_STOCK, RESERVED, IN_PRODUCTION, QUARANTINE, SOLD, RETURNED, WRITTEN_OFF }
